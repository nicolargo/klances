apiVersion: v1
kind: Service
metadata:
  name: {{ include "klances.fullname" . }}
  labels:
    {{- include "klances.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "klances.selectorLabels" . | nindent 4 }}
